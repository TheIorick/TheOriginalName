Таблица с произвольным количеством строк и столбцов. Реализовать методы
для произвольной выборки данных из таблицы по столбцами и по строкам,
группироваку данных по столбцам и по строкам.

Класс таблица
Класс методов
Класс с выводом данных

///////////////////////////////////////////////////////////////////////////////////
Добрый день, уточнил по поводу задачи.
В общем и целом могу сказать что нужно сделать что-то похожее на эксель.
1. Да, таблица должна быть динамическая
2. Все те же данные которые поддерживаются в экселе типа чиселки, строки и даты
3,4. Все как в экселе

Еще желательно показать что мы можем из csv загрузить нашу таблицу и выгрузить
в csv(простой формат в котором у нас все значения построчно с разделителями в виде ; или , лежат)

Можно посмотреть как работают Apache POI или JExcel и сделать некий простой аналог
/////////////////////////////////////////////////////////////////////////////

Таблица - лист листов... мб массив? но тогда динамически не получится, да и по строкам/столбцам сложна будет...
Короче, лист - значения одной строки, в листах листы строк
Таблица. Объект лист - который по своей сути является листом листов, т.к. мы делаем динамическую .....
методы добавить строку, добавить строку по номеру -- createRow();
методы добваить столбец, добавить столбец по какому-то номеру строки -- createColumn();
метод, который создает ячейку.. хм... грубо говоря, он проходит по

Реализация по пунктам.

0.+ Основа - ячейка
1+.Основа - строки<столбцы(значения)>
2+.Методы создания новых строк
3+. Методы создания новых столбцов
4+. Поиск запрашиваемой ячейки
5+. Установка значений в ячейку

ВЫВОД(?) Форматирование
Вывод производить в консоль сложно, т.к. не смогу выровнять строки и столбцы, но на жто можно забить болт.
Просто выводить построчно через запятую. Может быть с номером строки-столбец
Для ввода использовать
номер строки, номер столбца, и стринговое значение. можно сохранить в файл.. мб как-то через сканер сделать пользовательский интерфейс.
разьеб
7. Запись в файл. Проходимся по всей таблице. Значения форматируем в строку. Между значениями запятые
7.1 Одну строку записывать целиком в строку.. потом её отправлять в файл..
7.2 построчно отправить всю таблицу в сsv файл.



\\\\\\\\\Мысли насчет консоли+++\\\\\\\\\\
Всё просто (нет)
надо как-то узнать кол-во символов в ячейке
узнаем все в столбце
берем самую длинную
по ней строим столбец одинаковой длинны по самой длинной, остлаьные просто добавляем пробелов..
далее перегородочка... следующий столбец..
строить по столбцам - хана..
... 									
сначала пробегаемся по всем столбцам.. для каждого столбца находим макс длинну. 
далее строим построчно зная какое у нас количество символов для каждого столбца....

.....Вывод одной строки....
выводим значения столбца, далее добиваем пробелами до макс длинны, далее перегородочка, и след значение


*Визуализация через консоль
7. Фильтрация - выбираешь столбец и передаешь в него все значения которые нужно скрыть
8. Группировка - передаешь в номера столбцов или строк которые нужно сгруппировать (сгруппированные штуки можно показать и скрыть)

//////// Мысли насчет фильтрации/////////
класс фильтр - хранит булеан значение вкл/выкл, хранит номер столбца, лист со всеми ячейками столбца и лист, который надо будет показывать
метод - включить фильтр, в который передается номер столбца для которого будет работать фильтр.
В него передаются все ячейки которые хранит этот столбец
Так же он сохраняет номер столбца.

метод (в outputConsole)+
Есть возможность напечать весь столбец с его номером (в строку(?) + номера соответствующие номера строк), т.е все ячейки столбца, чтобы видеть
что скрыто, а что ещё нет

метод+
Передаем ячейки, КОТОРЫЕ БУДЕМ ПОКАЗЫВАТЬ (По номерам строк)... по номеру находится клетка.
Она добавляет в лист с клетками которые надо показывать
Делаем метод который пробегается по списку и если находит там нужную клетку, то он возвращает true, печатаем строку в которой есть наша ячейка, иначе скип

метод+
Передаем номер ячейки, которую хотим скрыть
она удаляет из листа эту ячейку

/////////Групппировка++++/////////
класс - булеан вкл/выкл для строк, булеан вкл/выкл для столбцов, для строки: верхний и нижний номер, для столбца правый и левый

метод
группирует по строкам: принимает два номера и сохраняет.. если больше верхнего и меньше нижнего, то скип...

метод
группирует по столбцам: принимает два номера и сохраняет.. если больше левого и меньше правого, то скип...

//////////Взаимодействие с пользователем/////////
Он может
создать таблицу любого размера
Добавить в ячейку значение по номеру строки.
можно добавить стринг, проверять на дабл и на localDate

Включить фильтр для н-го столбца
выключить фильтр..
можно конечно сделать фильтр и для строки, но такого в екселе не было, так хрен с ним

Сделать группировку по строкам - столбцам
отменить группировку

сохранение в файл.....

зараза..

/////////////////////              Даты              /////////////////////
Ввод:
Дата, время, Дата и время
Отдельный метод на проверку форматы даты, какой формат даты установлен
метод для установки нового формата
формат дат хранить.

можно парсить из стринга в дату
по основным форматам






//Дефолтный формат
Итог с 12 минуты
--Исправить парс инта +-
--Добавить поддержку дабла +-
--Добавить поддержку дат (хранить в истантах мб)
--Добавить дефолтных форматов и изменение форматов
--Избавиться от Джейнерика в Тэйбл Селл +-
 мб Использовать полиморфизм и всю эту штуку
-- добавить возможность свапа строк и столбцов




